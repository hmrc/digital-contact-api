Various digital contact APIs reside within services such as secure-message, preferences, and email. 
These APIs will be exposed to other HMRC services via the Developer Hub API.

GET /messages/count

GET /paperless/preferences